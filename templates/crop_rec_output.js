// // Get all the images in the table
// const images = document.querySelectorAll('img');

// // Add a click event listener to each image
// images.forEach(image => {
//   image.addEventListener('click', () => {
//     // Get the crop name from the sibling td element
//     const cropName = image.parentElement.nextElementSibling.textContent;
//     // Display the crop name in an alert
//     alert(`You clicked on ${cropName}`);
//   });
// });
